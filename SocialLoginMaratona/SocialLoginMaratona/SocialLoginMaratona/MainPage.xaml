<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SocialLoginMaratona"
             x:Class="SocialLoginMaratona.MainPage"
             Title="{Binding Title}">
    <ScrollView>
        <StackLayout Padding="60" HorizontalOptions="Center" VerticalOptions="CenterAndExpand">
            <Label Text="Id do usuario" TextColor="Black" FontSize="Medium"/>
            <Label Text="{Binding UserId}" TextColor="Gray" FontSize="Small"/>
            <Label Text="Token do provedor" TextColor="Black" FontSize="Small"/>
            <Label Text="{Binding Token}" TextColor="Gray" FontSize="Medium"/>
        </StackLayout>
    </ScrollView>


</ContentPage>